# 默认使用 8014，避免和旧的 8000 mock 后端冲突。
PORT=8014

# 不要把真实 API Key 写入代码仓库；请只在本地 .env 中填写。
LLM_API_KEY=请在本地填写真实API密钥

# 如果本机无法访问 api.openai.com，请使用可访问的中转平台 Base URL。
LLM_BASE_URL=请填写你的API平台BaseURL，例如 https://api.openai.com/v1 或中转平台URL

# 模型名必须以 API 平台后台显示为准。
LLM_MODEL=请填写API平台支持的模型名
